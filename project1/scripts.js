// Storing user credentials let users = {};  // Function to handle signup function signup() {     const newUsername = document.getElementById("newUsername").value;     const newPassword = document.getElementById("newPassword").value;      if (newUsername in users) {         document.getElementById("signupMessage").textContent = "Username already exists!";     } else {         users[newUsername] = newPassword;         document.getElementById("signupMessage").textContent = "Signup successful!";     } }  // Function to handle login function login() {     const username = document.getElementById("username").value;     const password = document.getElementById("password").value;      if (username in users && users[username] === password) {         document.getElementById("loginMessage").textContent = "Login successful!";         window.location.href = 'main.html';     } else {         document.getElementById("loginMessage").textContent = "Invalid credentials!";     } }  // Redirect to login/signup on Get Started function getStart() {     let choice = confirm("Would you like to Login or Signup?");     if (choice) {         window.location.href = 'login.html';     } else {         window.location.href = 'signup.html';     } }      